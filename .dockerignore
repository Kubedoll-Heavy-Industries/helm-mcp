# .git is intentionally NOT excluded: the Go toolchain stamps VCS
# revision/time into the binary (-buildvcs) so container builds are
# self-identifying. Only the build stage sees it; runtime is distroless.
.github
.devcontainer
.vscode
.idea
*.iml
.DS_Store

/tmp
/tmp/**
tmp/
tmp/**

**/*_test.go

dist

node_modules
e2e-ts/
mcp-helm
coverage.out
*.svg
*.png
pnpm-lock.yaml

*.log
build-errors.log

cloudflare/
